platform :ios, '12.0'

# Uncomment the next line if you're using Swift or would like to use dynamic frameworks
use_frameworks!

target 'Runner' do
  # Remove the manual Flutter pod reference:
  # pod 'Flutter', :path => '../../.flutter-plugins'

  # Flutter automatically handles the required dependencies now
  flutter_application_path = '../../'
  load File.join(flutter_application_path, '.flutter-plugins')
end
